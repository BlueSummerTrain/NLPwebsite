runserver:
	./start.sh
requirment:
	apt-get install mongodb
	pip install pymongo
	pip install django
	pip install mongoengine

WebBrowser way:
	$server_ip:$port
using url way:
	$server_ip:$port/dev/?sentence=$sentence_data


